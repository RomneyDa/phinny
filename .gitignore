# Generated Xcode project (regenerated by `xcodegen generate`)
Phinny.xcodeproj/

# XcodeGen generates this from project.yml's info.properties
Resources/Info.plist

# Build products
.build/
build/
dist/

# Signing credentials - only the *.example is tracked
.env.signing.local

# Dev SimpleFIN token - only .env.example is tracked
.env

# SwiftPM
.swiftpm/

# macOS cruft
.DS_Store

# Docs site local tooling (the docs are plain static HTML, but ignore caches)
docs/node_modules/
